import React, { Fragment } from "react";
import { Row, Col } from "react-bootstrap";

export class Datasource extends React.PureComponent {
  render() {
    return (
      <Fragment>
        <h1>Datasource</h1>
      </Fragment>
    );
  }
}

export default Datasource;
